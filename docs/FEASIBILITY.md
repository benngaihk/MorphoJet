# Feasibility

The original feasibility report is tracked at the repository root as `MORPHOJET-FEASIBILITY.md`.

Short version: build the narrow measurement-only CellProfiler-compatible subset first. Do not build a GUI, segmentation engine, or complete CellProfiler replacement for M0.
