# Production Readiness

MorphoJet is not production-grade until this checklist is satisfied by current code, current tests, and current release artifacts.

## Release Contract

Production-grade means:

- A tagged release can be built from CI.
- macOS and Linux binaries are published with checksums.
- The CLI exits non-zero on invalid inputs and never silently overwrites existing measurement outputs.
- Output CSV files are complete for the successful run or absent for a failed run.
- Every public compatibility claim is tied to a parity report.
- Every performance claim is tied to a benchmark report.

## Required Gates

| Area | Gate | Current Status |
|---|---|---|
| CLI safety | Validate required paths and image-table schema, reject empty image tables, reject invalid thread counts, protect existing outputs unless explicitly overwritten | Implemented for current CLI |
| Output safety | Avoid partial final `Image.csv` / `Objects.csv` files on failure and prevent diagnostic reports from masquerading as measurement CSVs | Implemented through staging writes plus final-target and report-target preflight checks for current CLI |
| Correctness | CellProfiler oracle parity report for public data | L2 ExampleHuman PASS and L3 CellBinDB direct-mask PASS for the current measurement subset |
| Testing | Unit, integration, CLI failure-mode, Clippy, Python helper tests, benchmark smoke tests, and scheduler-ready L3 validation | Implemented for current CLI; CellBinDB L3 validation script, scheduled GitHub workflow, and L3 provenance/hash gate added |
| Performance | Synthetic regression benchmark plus real CellProfiler benchmark | L3 CellBinDB benchmark PASS: 609.82x speedup, 13.98% RSS ratio |
| Workflow fit | CellProfiler-style object CSV handoff can run without manual CSV editing | CellBinDB L4-preflight handoff PASS with 35 contract columns; optional `measure --include-object-metadata` repeats image-table metadata such as plate/well/site on each object row for downstream grouping while the default `Objects.csv` schema remains unchanged; the external L4 template declares required object metadata columns, readiness fails before trial execution if MorphoJet `Objects.csv` does not include them, the wide-export bridge carries declared metadata columns into downstream CSVs while treating them as explicit pass-through fields during subset comparison, evidence-package READMEs render plus gate-check the handoff contract for reviewers, saved package verifier reports bind the README contract back to the rendered manifest, and local evidence preflight carries the same README handoff contract plus package reviewer entrypoint into saved input artifacts and Markdown review output; external trial template and release-gate external trial report plus strict-runner metadata, clean-git current-or-compatible commit metadata, reviewer signoff metadata, manifest-declared step command/runtime/detail/artifact coverage, and one-to-one artifact hash validation implemented |
| Observability | Clear stderr summary, actionable error context, runtime diagnostics, and machine-readable success/failure metadata | Runtime `doctor`, optional `measure --summary-json`, and optional `measure --error-json` with basic error codes implemented |
| Release | GitHub release workflow and checksums | `v0.1.0-rc.1` prerelease PASS: GitHub Actions built Linux/macOS archives, checksums verified, macOS packaged `doctor` commit verified; release archives now include `README.md`, `README.zh-CN.md`, and `LICENSE`, and local plus GitHub release verifiers require the Chinese README before a stable release can satisfy release evidence; generated external L4 stable-release verification now passes `--expect-commit <trial_plan git_commit>` and saved stable-release report review must use the same `--expect-commit`, so the stable tag, archives, `doctor` commit prefix, saved release report, and saved workflow evidence must bind to the same planned commit; release gate reports now record git commit, dirty-worktree status, arguments, optional L3 provenance/hash validation, and a production-claim audit |
| Documentation | Supported inputs, unsupported scope, parity gaps, and production caveats documented | L3 evidence and release gate documented; saved release-gate report verification, local-preflight skipped-final checklists, production-evidence-audit reports, final/non-final claim-scope labels, external L4/readiness/package/reviewer/stable-release/local-preflight non-final claim-scope labels, production audit status contracts, release-gate production evidence path contracts, planned stable tag/URL/semver matcher, and final production PASS gate-name requirements reuse release-gate-owned production audit contracts directly, so writer/reviewer/preflight/final-pass production-claim checklists, intermediate evidence labels, production status interpretation, production evidence path validation, and stable-release identity checks cannot drift; production-evidence-audit reports now record file summaries plus directory `input_artifacts` for `external_trial_root` and `external_evidence_package_dir`, so saved-audit rechecks catch final-input directory replacement before final wrapper signoff; generated external L4 trial plans now derive final signoff requirements, production blocker planned paths, and final production wrapper flag bindings from one shared final-gate binding source; final production wrapper, saved release-gate verifier, external L4 plan generator, and GitHub release verifier default all reuse the release-gate production repo identity for stable-release validation, and final wrapper plus saved release-gate verifier command checks use the same canonical saved reviewer and release verifier command builders as direct release-gate reports; source-doc claim language guard scans top-level Markdown plus recursive `docs/` and `corpus/` English/Chinese docs, rejects unsupported production-ready, production-grade, production-level, or CellProfiler-replacement claims, enforces the root bilingual README contract so `README.zh-CN.md` keeps the external L4 workflow, local preflight, final production wrapper, current blockers, package README evidence, README Chinese-version maintenance commitment, and the Chinese-community review entrypoint visible, now requires both root READMEs to preserve shared audit anchors such as non-final claim labels, `production_claim_status=INCOMPLETE`, the external L4 workspace command, final wrapper command, `Verify saved external L4 reviewer report pair`, saved stable-release report signoff flags, and saved final wrapper report flags, and requires this production-readiness document to keep the stable saved-report signoff shape with `--require-stable-report`, `--expect-tag v0.1.0`, `--expect-repo benngaihk/MorphoJet`, and `--expect-commit <final-commit>`; generated external L4 workspace READMEs also preserve the same Chinese reviewer entrypoint, render release-gate-derived machine-readable `production_claim_blockers`, state that saved reviewer report signoff must pair `--require-report-pass` with `--verify-report-files` and that external trial/package saved reviewer report signoff must also use `--expect-commit <trial_plan git_commit>`, state that live stable release verification and saved stable-release report review must use `--expect-commit <trial_plan git_commit>`, are rechecked by saved trial-plan verification, and now have an explicit shared-anchor guard requiring both languages to retain non-final labels, saved reviewer report flags, local-preflight signoff, final wrapper saved-report flags, and final saved-report verification flags; evidence-package READMEs preserve a package-level Chinese reviewer entrypoint, and saved package verifier reports record plus recheck it; L4 workflow caveats remain |

## Production Hardening Backlog

Priority order:

1. Prepare an external trial workspace with `benchmark/prepare_external_l4_trial.py --workspace path/to/external-trial`, which refuses stale trial, reviewer, preflight, stable-release verifier, final production-claim, or package outputs before writing the scaffold; fill the copied manifest's required external evidence block, place the real batch handoff files, preserve the plan's UTC `generated_at_utc`, `git_commit`, canonical generator `argv`, absolute template/workspace/manifest paths, template hash, `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_TRIAL_PLAN`, `final_production_signoff=false`, `external_evidence_requirements` binding the reviewer/signoff field list, UTC review timestamp, `manual_csv_editing=false`, and at least 3 acceptance criteria, `pre_signoff_requirements` binding readiness, saved reviewer-report rechecks, production evidence audit, and local-preflight prerequisites to planned paths, verification steps, and the later step each prerequisite is required before, and `final_signoff_requirements` binding final artifacts to planned paths, verification steps, and the exact final wrapper argument each artifact satisfies: `final_production_gate --external-trial-json`, `--external-evidence-package-dir`, `--external-trial-verification-report`, `--external-evidence-package-verification-report`, `--github-release-tag`, `--github-release-verification-report`, `--github-workflow-verification-report`, and `--production-evidence-audit-report`, including a distinct `stable_github_release` planned path for `https://github.com/benngaihk/MorphoJet/releases/tag/v0.1.0`, a separate `stable_github_release_saved_report` verifier JSON, a live stable-release verifier command bound with `--expect-commit <trial_plan git_commit>`, a saved stable-release verifier report recheck bound with `--expect-commit <trial_plan git_commit>`, a saved GitHub Actions workflow verifier JSON bound with `--commit <trial_plan git_commit>` and rechecked with `--expect-commit <trial_plan git_commit>`, and a saved production evidence audit JSON, run the generated `verify_plan --verify-plan-files --require-plan-files` command first so the saved plan schema, generator command binding, regenerated command set, regenerated external evidence requirements, regenerated pre-signoff requirements, regenerated final signoff requirements, readiness/local-preflight planned path command bindings, saved reviewer-report preflight-prerequisite bindings, external evidence command-flow bindings, saved trial/package reviewer-report recheck bindings, stable release tag/repo/report/commit identity bindings, GitHub workflow report repo/branch/commit binding, production evidence audit binding, final-signoff `required_for` flags, final production command flag values, UTC timestamp, template hash, manifest presence, and English plus Chinese README contents are re-checked before any external L4 execution step; the saved plan verifier rejects `--require-plan-files` without `--verify-plan-files`, so a structurally valid plan cannot be accepted without file-level rechecks. Those generated READMEs must also render the external evidence requirements, preserve that saved package verifier reports are `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_EVIDENCE_PACKAGE_REVIEW`, and `final_production_signoff=false`, state that saved trial-plan signoff must pair `--require-plan-files` with `--verify-plan-files`, state that the live stable-release verifier and saved stable-release report verifier must use `--expect-commit <trial_plan git_commit>`, state that saved trial/package reviewer reports must be rechecked with `verify_trial_report` and `verify_package_report` before local preflight or final signoff treats them as reviewer evidence, state that saved-report signoff must pair `--require-report-pass` with `--verify-report-files`, preserve that saved production evidence audits must be rechecked with `--verify-report-files --require-ready`, and preserve that saved local preflight reports are `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=LOCAL_EXTERNAL_L4_PREFLIGHT`, and `final_evidence_acceptable=false` with verifier file rechecks for source/package trial claim-scope labels, package-manifest package/source-trial scope labels, readiness READY status/claim-scope/generated-time/`package_name`/workspace/manifest fields, package README-rendered readiness scope, package README-rendered handoff contract, package README `review_entrypoint_present` values, and the saved local preflight Markdown `Review Entrypoint` column. Run the generated readiness audit with `benchmark/check_external_l4_readiness.py` so the saved `trial_plan.json`, template hash, manifest presence, English and Chinese README contents, input CSV schemas, declared object set and channels, manifest-declared trial outputs, and planned reviewer/preflight/package outputs are all checked before execution; preserve the readiness report's UTC `generated_at_utc`, absolute workspace/manifest paths, canonical package-name slug or null, canonical checker `argv` with absolute saved `--json-out`, `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_READINESS_PRECHECK`, and `final_production_signoff=false` for audit, re-check the saved readiness report with `--verify-report-files --require-ready` so the saved package-name field, argv binding, saved plan, and bilingual README contents are all validated, then run the generated `benchmark/run_handoff_trial.py --require-external-evidence --readiness-report path/to/readiness.json` command so the runner verifies the READY report before execution, rejects a report whose manifest or workspace does not match the current trial manifest and `base_dir`, binds its size/SHA-256/status/claim-scope labels/timestamps/package-name/workspace/manifest summary into the trial report, and labels the trial report itself as `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_WORKFLOW_TRIAL`, and `final_production_signoff=false`. Validate the resulting report with release gate, re-check the saved trial reviewer report with file hashing and PASS enforcement, package it with `benchmark/package_external_trial.py`, preserve the package artifact manifest's UTC `packaged_at_utc`, `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_EVIDENCE_PACKAGE`, `final_production_signoff=false`, copied `readiness.json`, source trial claim-scope labels, and bilingual README review files, re-check the saved package reviewer report with file hashing, PASS enforcement, and source-trial binding, run the generated saved local-preflight verifier so the preflight report is rehashed, gate-rechecked, readiness READY status/claim-scope/generated-time/package-name/workspace/manifest-recomputed, source/package trial scope-recomputed, package-manifest scope-recomputed, package README scope, handoff-contract, and reviewer-entrypoint status recomputed and rendered in Markdown, and PASS-enforced before review/signoff, then after stable `v0.1.0` exists run the generated stable-release verifier, saved stable-release report verifier with expected tag, expected repo, and expected commit binding, saved GitHub workflow verifier, production evidence audit plus saved audit verifier, final production wrapper command, and final saved production-claim report verifier from the same saved plan.
Readiness signoff note: saved readiness reports must be rechecked with both `--verify-report-files` and `--require-ready`, and the verifier rejects `--require-ready` by itself so stale saved READY JSON cannot become reviewer-ready evidence without revalidating the current workspace files.

2. Promote the release-candidate validation path from `v0.1.0-rc.1` to a stable `v0.1.0` tag after external workflow evidence is accepted by `benchmark/release_gate.py --external-trial-json`, then verify it with `benchmark/release_gate.py --verify-github-release v0.1.0 --github-release-kind stable`.
3. Broaden the supported measurement subset beyond the current intensity and size/shape columns.

After items 1 and 2 are complete, run the final production wrapper:

```bash
python3 benchmark/run_production_gate.py \
  --external-trial-json path/to/external/handoff_trial.json \
  --external-trial-root path/to/external \
  --external-evidence-package-dir path/to/evidence-packages/external-l4-trial \
  --external-trial-verification-report path/to/external/handoff_trial-verification.json \
  --external-evidence-package-verification-report path/to/evidence-packages/external-l4-trial-verification.json \
  --github-release-verification-report path/to/github-release/verification.json \
  --github-workflow-verification-report path/to/github-workflows.json \
  --production-evidence-audit-report path/to/production-evidence-audit.json \
  --github-release-tag v0.1.0
```

This is the single command intended to produce and re-check the final production-claim report. It requires a stable non-RC tag and fails fast unless five saved final-input report arguments are supplied, including for `--dry-run`: `--external-trial-verification-report`, `--external-evidence-package-verification-report`, `--github-release-verification-report`, `--github-workflow-verification-report`, and `--production-evidence-audit-report`. It checks that the external evidence paths and saved verifier/audit reports exist before an actual non-dry-run final run, re-checks the saved verifier reports, reruns the saved production evidence audit with `--verify-report-files --require-ready`, requires saved package reviewer reports to be bound to a source `trial_json`, requires saved trial/package reviewer reports to match the current external trial JSON, trial root, evidence package directory inputs, current final commit through `--expect-commit`, and the same external-evidence identity summary, requires the saved GitHub release verification report to be a stable PASS report for the same final tag, final commit, and the `benngaihk/MorphoJet` repo, requires the saved GitHub workflow report to bind `benngaihk/MorphoJet`, `main`, the current git commit, `ci.yml`, and `external-l4-rehearsal.yml`, rejects final report outputs that would overwrite packaged `readiness.json`, package review files including `README.md` and `README.zh-CN.md`, or create files inside protected external evidence paths, passes those reviewer-report checks into the final release-gate JSON/Markdown, delegates to `benchmark/release_gate.py --require-production-claim`, and if that gate succeeds immediately re-checks the saved final report with `benchmark/verify_release_gate_report.py --require-report-pass --require-clean-git-metadata --verify-git-commit --require-production-claim-pass --expect-missing-checks none`, so production remains incomplete until that command passes with real external evidence, saved external L4 reviewer reports, a live stable release verification, a saved stable-release verifier report, saved GitHub workflow evidence, a ready saved production evidence audit, top-level `claim_status=FINAL_PRODUCTION_CLAIM`, `evidence_scope=FINAL_PRODUCTION_RELEASE_GATE`, `final_production_signoff=true`, and a self-verified final saved report. The saved final report verifier now rejects `--require-production-claim-pass` unless it is paired with `--require-report-pass`, `--require-clean-git-metadata`, `--verify-git-commit`, and `--expect-missing-checks none`, so final signoff cannot skip whole-report PASS, clean committed metadata, reachable commit, or zero-blocker enforcement. Direct release-gate live GitHub release verification must bind the command to `benngaihk/MorphoJet`; direct `benchmark/release_gate.py --require-production-claim` also fails at argument validation unless `--require-clean-git` and `--require-l3-provenance` are both supplied, so the direct path cannot skip clean-worktree and L3 provenance enforcement before evaluating final evidence. Direct saved reviewer-report gates also bind saved external trial/package reports to the current external evidence inputs, current final commit, and matching external-evidence identity, re-check saved GitHub release reports with `--verify-git-commit`, bind saved GitHub reports to the supplied live release tag when `--verify-github-release` is present, require `--expect-repo benngaihk/MorphoJet`, and reject report output paths that would overwrite package `readiness.json`, external evidence inputs, package files/artifacts, reviewer reports, or the GitHub release verifier report. `--dry-run` may still print the assembled commands before supplied paths exist, and `--local-evidence-preflight-only` remains the earlier non-final path where external saved reviewer reports may be incomplete.

If the external L4 evidence is ready before the stable release, run the same wrapper with `--local-evidence-preflight-only` to validate only the external trial report, evidence package, and supplied external L4 saved reviewer verifier reports, then write a local evidence-preflight JSON/Markdown report. That report is machine-labeled `NOT_PRODUCTION_CLAIM`, `evidence_scope=LOCAL_EXTERNAL_L4_PREFLIGHT`, and `final_evidence_acceptable=false`, lists the skipped final checks, records a machine-checkable `skipped_final_checklist` with required evidence and next actions, and binds the key evidence files by absolute path, size, and SHA-256, including the source trial JSON and package copy of `handoff_trial.json` plus their `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_WORKFLOW_TRIAL`, and `final_production_signoff=false` labels, the package `artifact_manifest.json` package-level and source-trial claim-scope labels, the package copy of `readiness.json` plus its READY status, `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_READINESS_PRECHECK`, `final_production_signoff=false`, UTC generation time, package-name, workspace, and manifest summary, and the package `README.md` plus `README.zh-CN.md` with their package claim-scope labels, README-rendered readiness scope fields, and README-rendered handoff contract fields. The Markdown report also renders those readiness fields in the input-artifact table and the README handoff contract in a dedicated table for human reviewers. Those required input-artifact summaries must remain `exists=true` with size/SHA-256 values in the saved report. Only if both saved external reviewer reports are supplied and the saved trial verifier, saved package verifier, and reviewer-report pair gates pass does the report record `external_l4_saved_reviewer_reports` in `validated_checks`; otherwise the same check remains in `skipped_final_checklist`, and saved preflight verification requires any metadata-bound saved reviewer gate entries plus their input-artifact hash summaries to remain present. Passing that preflight reduces L4 packaging risk, but it still does not satisfy the live stable-release, saved stable-release verifier, or final production-claim enforcement gates.

After local preflight, use `benchmark/audit_production_evidence.py` as a lightweight final-input inventory. Its saved report is `schema_version=2` and is labeled `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=PRODUCTION_EVIDENCE_READINESS_AUDIT`, and `final_production_signoff=false`; it audits clean git, L3 provenance, saved GitHub workflow evidence, external L4 trial/package/reviewer evidence, live stable release verification when `--verify-live-github-release` is supplied, and the saved stable-release verifier report. It records `input_files` size/SHA-256 summaries for the external trial JSON and each supplied saved final-input report, and `input_artifacts` directory summaries for `external_trial_root` and `external_evidence_package_dir`. When both saved external reviewer reports are present, the saved audit must retain the saved trial verifier, saved package verifier, and reviewer-report pair gate entries before the reviewer evidence can be counted ready. Re-check the saved audit with `benchmark/audit_production_evidence.py --verify-report path/to/production-evidence-audit.json --verify-report-files --require-ready` before running the final wrapper. The final wrapper now requires the same saved audit through `--production-evidence-audit-report` and reruns that signoff-mode recheck before invoking the release gate. This recheck requires the saved audit to bind to the current git commit, absolute metadata argv paths, the verified audit JSON path, the same final-wrapper inputs, current input-file hashes, current input directory existence/type, required saved-reviewer gates, and freshly recomputed audit gate statuses. This report can catch missing paths or stale saved verifier evidence earlier, but it is still only a reviewer checklist and cannot replace the final wrapper or saved final release-gate verifier.

Generated external L4 trial plans now include `audit_production_evidence` and `verify_production_evidence_audit` as explicit steps between saved workflow/stable-release verification and `final_production_gate`, and the saved plan verifier checks that the audit consumes the same final input paths, uses `--verify-report-files --require-ready`, and is passed into `final_production_gate` with `--production-evidence-audit-report`.

Direct final-claim release-gate use must bind external L4 evidence as a complete input group. `--external-trial-json` requires `--external-trial-root`; trial evidence also requires `--external-evidence-package-dir`; `--external-evidence-package-dir` requires `--external-trial-json`; complete external L4 evidence requires both saved reviewer reports; `--external-trial-verification-report` requires `--external-trial-json`, `--external-trial-root`, and `--external-evidence-package-verification-report`; `--external-evidence-package-verification-report` requires `--external-evidence-package-dir`, `--external-trial-json`, and `--external-trial-verification-report`; live GitHub release verification rejects prerelease/RC gates unless `--github-release-kind stable` is supplied; and `--github-release-verification-report` is rejected unless the same command also supplies `--verify-github-release`. This matches the final wrapper's stable non-RC release and evidence-binding requirements, keeping partial external evidence, unreviewed external L4 evidence, half-reviewed external L4 evidence, or saved-release evidence out of the final production-claim path.

Direct final-claim release-gate use also requires stable-release evidence to be present, not merely bound when supplied: `--require-production-claim` requires live `--verify-github-release`, requires `--github-release-kind stable`, and requires `--github-release-verification-report` in the same command. This keeps missing live stable-release evidence or missing saved stable-release verifier evidence out of the final production-claim path before heavier audit checks run.

Direct final-claim release-gate use also requires the external L4 evidence group to be present before heavier audit checks run: `--external-trial-json`, `--external-trial-root`, `--external-evidence-package-dir`, `--external-trial-verification-report`, and `--external-evidence-package-verification-report` are all mandatory with `--require-production-claim`, and an isolated `--external-trial-root` is rejected unless the same command supplies `--external-trial-json`. This keeps missing external L4 workflow, package, or saved reviewer evidence out of the final production-claim path before the release gate spends time on downstream audit work.

Use `benchmark/run_production_gate.py --verify-local-evidence-preflight-report path/to/local-evidence-preflight.json` to re-check the saved local evidence-preflight report's schema, UTC `metadata.generated_at_utc`, reachable git commit, stable non-RC `metadata.github_release_tag`, claim-scope labels, final-evidence rejection flag, skipped-final checklist, non-empty absolute metadata evidence paths, required `exists=true` input artifacts, unique absolute input artifact paths, absolute path-valued `metadata.argv` entries, metadata-to-input-artifact path bindings for the trial JSON, package trial JSON, package artifact manifest, package readiness JSON, package `README.md`, package `README.zh-CN.md`, package zip/checksum, and optional reviewer reports, package-readiness READY status/claim-scope/generated-time/package-name/workspace/manifest binding when the referenced package is available, package-README package/readiness scope and handoff-contract binding, gate entry shape, unique gate names, required saved reviewer gate entries when reviewer metadata paths are present, and consistency between top-level status and recorded gate statuses during review. Add `--verify-local-evidence-preflight-files` when the referenced evidence files are available so the recorded input file sizes, SHA-256 hashes, source/package trial claim-scope labels, package artifact-manifest package/source-trial claim-scope labels, package-readiness READY status/claim-scope/generated-time/package-name/workspace/manifest summary, and package README scope plus handoff-contract summaries are recomputed. Add `--verify-local-evidence-preflight-gates` to rerun recorded external L4 trial, package, and saved reviewer-report gates from report metadata. `--require-local-evidence-preflight-pass` is only valid for review/signoff when the saved metadata has `git_dirty=false` and empty `git_status`, and both file rehashing and gate reruns are enabled.

Use `benchmark/verify_external_trial_report.py --verify-report path/to/trial-verification.json --verify-report-files --require-report-pass --expect-commit <final-commit>` during signoff to re-check the saved external L4 trial reviewer report. The saved report records `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_WORKFLOW_TRIAL_REVIEW`, `final_production_signoff=false`, canonical verifier `argv`, absolute source trial/root paths, source trial JSON size/SHA-256 plus `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_WORKFLOW_TRIAL`, `final_production_signoff=false`, and `metadata.git_commit`, bound readiness report size/SHA-256 plus READY status, `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_READINESS_PRECHECK`, `final_production_signoff=false`, UTC generation time, workspace, manifest, and package-name summary, and resolved artifact file size/SHA-256 summaries, requires the `argv` trial JSON, `--trial-root`, and `--json-out` path values to be absolute with `--json-out` bound to the saved report path under review, requires those summary paths, source trial claim-scope labels, source trial commit, and trial readiness status/claim-scope/generated-time/workspace/manifest/package-name fields to remain bound to the saved top-level trial JSON/trial root inputs and trial `readiness_report`, rejects reviewer report outputs that would overwrite or create files inside the source trial JSON, bound readiness report, or declared artifacts, and file recheck recomputes those summaries plus the external trial validation gate. `--require-report-pass` and `--expect-commit` are signoff-mode checks and now fail unless `--verify-report-files` is supplied. The production wrapper additionally rejects a saved trial reviewer report whose saved trial JSON, trial root, or source trial commit differs from the current wrapper inputs.

Use `benchmark/verify_external_evidence_package.py --verify-report path/to/package-verification.json --verify-report-files --require-report-pass --require-trial-json --expect-commit <final-commit>` during signoff to re-check the saved external L4 evidence-package reviewer report. The saved report must be bound to the source trial JSON, rejects unexpected saved input-file entries, records `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_EVIDENCE_PACKAGE_REVIEW`, `final_production_signoff=false`, canonical verifier `argv` plus absolute package/source-trial paths and size/SHA-256 summaries plus claim-scope labels and git commit for that source trial JSON, the package review files including the package copy of `handoff_trial.json` with `metadata.git_commit`, the package copy of `readiness.json` with its READY status, claim-scope, UTC generation time, package-name, workspace, and manifest summary and both English/Chinese READMEs with `trial_git_commit`, package zip, checksum file, and package artifact-manifest package-level and source-trial claim-scope fields, requires the `argv` package directory, `--trial-json`, and `--json-out` path values to be absolute with `--json-out` bound to the saved report path under review, rejects reviewer report outputs that would overwrite or create files inside source/package evidence files, and requires those top-level labels, summary paths, source-trial claim-scope labels and git commit, package trial git commit, package README trial git commit, package-readiness READY status, claim-scope, UTC generation time, package-name, workspace, and manifest fields, and package-manifest package-level/source-trial claim-scope fields to remain bound to the saved top-level package/source-trial inputs; file recheck recomputes those summaries, claim-scope fields, package README commit fields, and the package validation gate. `--require-report-pass` and `--expect-commit` are signoff-mode checks and now fail unless `--verify-report-files` is supplied. The package validation gate also requires the package READMEs to preserve the readiness package name, readiness workspace, readiness manifest, required signoff fields, non-final claim boundary, and trial commit; requires the package artifact manifest to preserve canonical packager `argv` matching the source trial JSON, trial root, output directory, and package name plus package-level and source-trial claim-scope labels; and requires the package to include a copied `readiness.json` whose bytes match the readiness summary bound into the trial report. The production wrapper additionally rejects a saved package reviewer report whose saved package directory, source trial JSON, or package/source trial commit differs from the current wrapper inputs.

Package README validation is bilingual: both `README.md` and `README.zh-CN.md` must render the copied readiness report's READY status, `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=EXTERNAL_L4_READINESS_PRECHECK`, `final_production_signoff=false`, UTC generation time, package name, workspace, and manifest before a package can satisfy the external evidence package gate. Saved package verifier reports also copy those README-rendered values into `input_files.package_readme` and `input_files.package_readme_zh`, then bind them back to the package READMEs, package artifact manifest, and copied `readiness.json` during saved-report review.

Use `benchmark/verify_github_release.py --verify-report path/to/github-release/verification.json --verify-report-files --require-report-pass --require-stable-report --verify-git-commit --expect-tag v0.1.0 --expect-repo benngaihk/MorphoJet --expect-commit <final-commit>` to re-check a saved stable-release verification report during signoff, or pass the same report to `run_production_gate.py --github-release-verification-report` so the final release-gate report records the saved-report gate. `--require-stable-report` is signoff mode and fails unless PASS enforcement, file rechecks, git commit/tag verification, expected tag, expected repo, and expected commit are all present. This recomputes downloaded asset names, archive SHA-256 values, checksum file contents, and recorded GitHub asset metadata identity/state/timestamp/size/digest fields from the absolute release output directory, rejects saved PASS reports for draft releases, requires saved reports to preserve `claim_status=NOT_PRODUCTION_CLAIM`, `evidence_scope=GITHUB_STABLE_RELEASE_VERIFICATION`, and `final_production_signoff=false`, requires saved reports to bind release and asset URLs to the saved repo/tag/asset names, requires the saved release API URL to match the recorded release database ID, requires `assets.expected` to match the required release assets for the saved tag, requires GitHub release ID/API identity plus release author, target commit-ish, immutable state, UTC verifier/release timestamps, requires unique GitHub asset IDs and API asset URLs bound to the saved repo, requires asset metadata timestamps to be UTC, requires canonical verifier `argv` to bind the saved tag, repo, absolute output directory, stable/prerelease expectation, absolute saved report path, and explicit `--expect-commit`, rejects saved reports whose recorded output directory would contain the saved verifier report or other non-release assets, requires PASS report archive summaries to record `checksum_match=true`, requires archive SHA-256 summaries to match the corresponding GitHub asset digest, requires saved reports to bind the full 40-character release commit to the 12-character `morphojet doctor` commit prefix, confirms the expected tag resolves to the saved commit in the current git checkout, and rejects saved reports whose recorded commit is not the supplied final commit; the production wrapper and direct release gate additionally reject a saved GitHub release report whose repo is not `benngaihk/MorphoJet`. This saved report does not replace `--verify-github-release` or the final production gate.

## Claim Policy

Until every required gate is complete, documentation may claim the narrow L3 benchmark result and the supported-column handoff preflight, but must not say "production-ready" or "replaces CellProfiler workflows" without an external workflow trial report and evidence package accepted by release gate.

The release-gate report's top-level `production_claim_status` is the machine-readable summary for this policy. It is expected to remain `INCOMPLETE` until clean-git, L3 provenance, external L4 workflow, external L4 evidence package, saved external L4 reviewer reports, stable GitHub release, and saved stable-release verifier checks are all present and passing in the same report. Reports also include top-level `claim_status`, `evidence_scope`, and `final_production_signoff`: every non-final report must remain `NOT_PRODUCTION_CLAIM`, `RELEASE_GATE_PRECHECK`, and `false`; a final production report must be `FINAL_PRODUCTION_CLAIM`, `FINAL_PRODUCTION_RELEASE_GATE`, and `true`. Reports also include top-level `missing_or_failed_checks`, which names the exact production-claim checks still blocking a production-ready or CellProfiler-replacement claim; JSON reports additionally include `production_claim_checklist`, and Markdown reports render that same checklist so every audit check maps to required evidence and next reviewer action for human signoff. Markdown reports also render a `Production Claim Boundary` section so copied reports remain clear to human reviewers: non-final reports say they are not production signoff, while final candidates say saved-report verification is still required. Review saved reports with `benchmark/verify_release_gate_report.py --verify-git-commit --expect-missing-checks ...` to require that the recorded commit is reachable, `metadata.generated_at_utc` is UTC, the blocker list is exactly the intended list for that milestone, and the top-level claim-scope labels match the final-signoff state; final production reports should use `--require-report-pass --require-clean-git-metadata --verify-git-commit --require-production-claim-pass --expect-missing-checks none`, and the verifier now rejects `--require-production-claim-pass` without that complete bundle. Final production/stable-release validation should run release gate with `--require-production-claim`; direct CLI use rejects that mode unless `--require-clean-git` and `--require-l3-provenance` are also present, and the resulting report still fails unless the production-claim audit is `PASS`. Saved final reports should also pass `benchmark/verify_release_gate_report.py --require-report-pass --require-clean-git-metadata --verify-git-commit --require-production-claim-pass --expect-missing-checks none`, which rejects production PASS reports with duplicate gate names, missing required production gate entries or final production metadata flags, missing or tampered production checklist rows or final claim-scope labels, true boolean metadata flags without matching gates, relative or missing external paths, external metadata paths without validation gates, external trial reports that lack a bound pre-execution READY readiness summary, missing saved reviewer/verifier report metadata, relative path-valued `metadata.argv` entries for external evidence, reviewer reports, or explicit report outputs, recorded `--out-json` values that do not match the report under review, live GitHub release gate commands that do not match the recorded tag/kind/output JSON and production repo, saved reviewer metadata paths without matching gates, saved reviewer gate commands that omit file rechecks, PASS requirements, package source-trial binding, external trial/package expected commit binding, stable release checks, git commit verification, expected tag binding, or expected final commit binding, stable release identity, clean-git metadata, a reachable report commit, UTC report metadata, or consistency between top-level status, recorded gate statuses, and recorded production-claim enforcement metadata. `release_gate.py` writes those production evidence path fields, argv path values, live GitHub verifier JSON paths, and saved reviewer gate command report paths as absolute paths so saved reports remain auditable outside the original shell working directory.

For direct CLI final-claim reviews, the same policy now fails fast before heavy gate execution when external trial/package inputs are supplied without their required partner paths, when a saved external trial/package reviewer report is supplied without the current external trial/package inputs, when `--require-production-claim` is paired with live `--verify-github-release` but the command does not use `--github-release-kind stable`, or when a saved `--github-release-verification-report` is supplied without a live `--verify-github-release` tag.
